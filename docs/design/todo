FIXME
    portchannels for baremetal
    default portchannels
    trunk groups (see LLD)
    SVIs for external networks
    ip address virtual on vlan $tenant_vlan_id

    dhcp relay for cc-mgmt networks

    DAPNets


    ACI handover (translit leaf)
    management networks
        need global vni

p. 70 (underlay/overlay configuration)
loopback0 per-device
    router-id, unique per device
loopback1
    vtep address, shared between leafpair / mlag pair
    only on leafs
loopback10 mgmt, per-device
